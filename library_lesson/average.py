import statistics  # importing statistics module

print(
    statistics.mean([100, 90])
)  # mean is a function that takes a list as an argument and returns the average of all the elements in the list
